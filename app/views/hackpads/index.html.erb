<div class='row'>
  <div class='col-md-8'>
    <div class='form-box'>
      <div class='form-box-title'>
        <h5>All yo pads.</h5>
        <div class='form-box-tools'>
          <%= link_to new_project_hackpad_path(current_project), class: 'btn btn-active btn-xs' do %>
            <span class="glyphicon glyphicon-plus" aria-hidden="true"></span> Add Hackpad
          <% end %>
        </div>
      </div>

      <div class='form-box-content'>
        <% if @hackpads.any? %>
          <ul class='sortable list-group'>
            <%= render @hackpads %>
          </ul>
        <% else %>
           <div class='empty-container'>
            <p>No Hackpads found.</p>
            <span>Use the add hackpad button above to get started.</span>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
